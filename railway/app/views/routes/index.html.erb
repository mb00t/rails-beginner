<p id="notice"><%= notice %></p>

<h1>Listing Route</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @routes.each do |route| %>
      <tr>
        <td><%= route.title %></td>
        <td><%= link_to 'Show Route', route %></td>
        <td><%= link_to 'Edit Route', edit_route_path(route) %></td>
        <td><%= link_to 'Destroy Route', route, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Route', new_route_path %>
